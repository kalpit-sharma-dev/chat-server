I am creating a agentic ai mobile app for retail banking.
I want to create a same UI as that of chatgpt for my mobile app in react native 
I already have rest api ready for me which I already use in my mobile banking app currently, I want to use the same api and build a conversational chatbot for our bank customer using agentic ai.


When you enter a prompt and hit send:

A POST request is made to the backend using fetch.

The response is streamed back as a text/event-stream (SSE-like) or using a ReadableStream of a chunked HTTP response.

Each chunk of the model's response is sent as it's generated, allowing for the "typing" effect in the UI.
Build this capability for me
